# OpenSN + LEO_TE 服务器迁移与实验交接

更新时间：2026-09-15
原环境：`/home/bai`，Ubuntu 22.04，4 GiB 内存虚拟机
后续环境：独立 Linux 服务器
GitHub 用户：`cookiebai`

---

## 1. 交接结论

本项目的 6×11 拓扑已经真实跑通过一套完整链路：

```text
OpenSN 节点与 OSPF
→ 拓扑嗅探和队列遥测
→ 无 TE/SRv6 策略的 Baseline 测量
→ Lyapunov 路径计算
→ SRv6 策略下发
→ IPv6 SID Ping
→ IPv4-over-SRv6 Ping
→ 相同流量条件下的 TE 测量
→ 提升百分比、稳定性和原始数据归档
```

6×11 最近两次完整单周期实验均成功。最终一次实测为：

- 68 个节点，246 条有向链路，全部为 `UP`。
- 24 条流量需求。
- Baseline：`54.910 Mbps`，CV `0.304%`。
- TE：`76.362 Mbps`，CV `0.355%`。
- 实测提升：`39.068%`。
- 策略：期望/已下发/队列剩余为 `24/24/0`。
- IPv6 SID Ping：`24/24`。
- IPv4-over-SRv6 Ping：`24/24`。
- Baseline 和 TE 的实际发送负载几乎一致，配对负载检查通过。

这证明了 6×11 下“算路、下发、节点安装 SRv6、按新路径通信、再测吞吐量”
的静态单轮链路已经完成。

12×10 拓扑的原始创建错误也已经定位并修复。错误：

```text
Add Topology Error: arrange instance error: unable to allocate instances
```

根因是 12×10 配置包含 `120 Satellite + 2 GroundStation = 122` 个实例，
而原 NodeDaemon 的 `instance_capacity` 只有 `100`。新增配置
`config/experiment_daemon_12_10.json` 后，容量提高到 `160`，并把创建并发
降到 `2`、关闭监控和 CodeServer。已经两次确认 API 的 update、topology、
start 都返回 `code=0`，且 122 个节点全部创建成功、没有退出节点。

但是，12×10 尚未在原虚拟机上完成 Standard 初始化、24 条 demand 发布、
SRv6 下发、Ping 和 Baseline/TE 吞吐量测量。原因不是拓扑配置错误，而是
4 GiB 虚拟机资源不足：122 个节点创建后内存余量很低、Swap 压力很高，
随后环境被停止。因此：

> 目前没有 12×10 的真实吞吐量结果，也不能宣称已经达到 12%～14%。
> 该部分必须迁移到服务器后继续做真实测量。

---

## 2. 两个 GitHub 仓库

本次交接代码使用同名分支：

```text
server-handoff-20260915
```

仓库地址：

```text
LEO_TE：
https://github.com/cookiebai/LEO_TE_Controller.git

OpenSN：
https://github.com/cookiebai/workspace.git
```

LEO 仓库远端原有的 `main` 与本地开发历史发生过分叉，因此本次不强制覆盖
远端 `main`。服务器必须显式克隆交接分支：

```bash
mkdir -p "$HOME/leo-te-lab"
cd "$HOME/leo-te-lab"

git clone --branch server-handoff-20260915 \
  https://github.com/cookiebai/workspace.git \
  OpenSN-Library

git clone --branch server-handoff-20260915 \
  https://github.com/cookiebai/LEO_TE_Controller.git \
  LEO_TE_Controller
```

克隆后核对：

```bash
git -C "$HOME/leo-te-lab/OpenSN-Library" status
git -C "$HOME/leo-te-lab/LEO_TE_Controller" status

git -C "$HOME/leo-te-lab/OpenSN-Library" branch --show-current
git -C "$HOME/leo-te-lab/LEO_TE_Controller" branch --show-current
```

两个分支都应显示：

```text
server-handoff-20260915
```

没有上传以下大文件，因为它们是构建产物、运行目录或可重新拉取的镜像，
不适合进入 GitHub：

- `opensn_build.tar.gz`，原文件约 2.1 GiB。
- `opensn_build/`。
- `NodeDaemon` 编译产物。
- Docker 镜像 tar 包。
- `ui/node_modules/` 和 `ui/build/`。
- OpenSN runtime 目录、日志和 Python 缓存。
- LEO 每次运行生成的 `results/runs/` 与 `results/latest`。

源代码、拓扑 JSON、配置、测试、关键历史结果和本文档均保留在仓库中。

---

## 3. 项目目标与实验原则

### 3.1 最终目标

1. 使用 OpenSN 创建 12×10 星座拓扑，即 120 个卫星和 2 个地面站。
2. OpenSN 生成节点、链路、OSPF 路由和 24 条实验流量需求。
3. 控制面读取真实拓扑和队列遥测，将拓扑转换为带权图。
4. Lyapunov/拥塞感知算法为 24 条流计算路径。
5. 把路径转换为 SRv6 SID 列表并下发到流量源节点。
6. 验证每一条策略对应的 IPv6 SID 和 IPv4-over-SRv6 都能 Ping 通。
7. 在完全相同的拓扑、需求、测量时间和发送负载下，比较：
   - Baseline：不运行控制面算法，不保留 SRv6 encap 策略。
   - TE：运行算法并完成 SRv6 下发。
8. 在服务器上调节总 offered load，使真实提升稳定落在 12%～14% 左右。
9. 固定参数后重复至少 3 个周期，保存所有原始数据和复现实验命令。

### 3.2 不能采用伪造数据

目标区间可以通过真实、可解释、对 Baseline 和 TE 完全一致的 offered load、
链路容量、场景和需求参数进行校准，但不能：

- 手工修改结果 JSON 中的吞吐量或提升比例。
- 给 Baseline 和 TE 使用不同的发送负载。
- 只保留命中目标的一次偶然结果并删除失败结果。
- 用理论值代替接收端的真实测量值。
- 把 6×11 的结果当作 12×10 结果。

脚本中的 `TE_TARGET_LOW_PERCENT` 和 `TE_TARGET_HIGH_PERCENT` 只负责定义验收区间，
不会修改吞吐量，也不会把区间外的结果伪装成区间内。

---

## 4. 整体系统框架

```text
OpenSN NodeDaemon
  ├─ 根据 emu_config 选择节点镜像和资源限制
  ├─ 根据 topology_config 创建 Satellite/GroundStation
  └─ 创建 veth、链路和容器网络
          │
          ▼
Standard TopoConfigurator
  ├─ 固定几何时间
  ├─ 初始化/更新链路
  ├─ 启动节点内 FRR/OSPF
  └─ 向 Redis 发布 24 条 te:demands
          │
          ▼
topo_sniffer.py + queue_monitor.py
  ├─ topo:link:*             真实链路、接口、容量、状态
  ├─ topo:last_update_unix   拓扑新鲜度
  └─ telemetry:queue:*       节点队列遥测
          │
          ▼
run_te_experiment.py
  ├─ 检查节点、OSPF、拓扑、遥测和需求
  ├─ 清除旧 SRv6/策略，测 Baseline
  ├─ 调用 lyapunov_solver.py
  ├─ 调用 sr_policy_sender.py
  ├─ 校验 24+24 次 SRv6 Ping
  ├─ 测量 TE
  └─ 校验 CV、配对负载、提升区间并写 JSON
          │
          ▼
run_full_experiment.sh
  ├─ 一键编排
  ├─ 日志与数据归档
  └─ 无论成功或失败都清理探针、临时文件和策略
```

### 4.1 Baseline 的定义

Baseline 不是“没有网络”，而是保留 OpenSN 数据面和 OSPF 的普通转发，只移除：

- 旧的 IPv4/IPv6 `encap seg6` 路由。
- Redis 中旧的 desired/applied policy。
- 旧求解器和策略发送器进程。

随后使用 24 条同步 UDP 流，在接收端统计有效吞吐量。

### 4.2 TE 的定义

在同一批流、相同目标速率和相同测量时长下：

1. 求解器从 Redis 读取 topology、telemetry 和 demands。
2. 计算每条流的单路径和逐跳 SID。
3. 策略发送器为节点准备 identity SID、transit SID、逐跳 IPv6 基础路由。
4. 在源节点安装 IPv6 和 IPv4 的 `encap seg6` 路由。
5. Ping 全部成功后，再进行 TE 吞吐量测量。

提升计算公式：

```text
(TE 接收端平均吞吐量 - Baseline 接收端平均吞吐量)
---------------------------------------------------------------- × 100%
             Baseline 接收端平均吞吐量
```

---

## 5. 已完成的主要工作

### 5.1 LEO_TE 控制和测量链路

关键文件：

- `run_full_experiment.sh`
  - 一条命令运行完整 Baseline → Solver → SRv6 → Ping → TE 流程。
  - 自动识别 66 或 120 个 Satellite，要求 2 个 GroundStation。
  - 自动启动且去重 `queue_monitor.py`。
  - 支持自定义 Python、OpenSN 路径、目标区间、总负载和测量时长。
  - 用 `trap` 保证正常结束、失败和 Ctrl+C 时都执行清理。
  - 每次运行保存独立目录，并更新 `results/latest`。

- `run_te_experiment.py`
  - 使用同步原生 UDP 探针，不依赖人工执行 `iperf`。
  - Baseline 前清除控制面和 SRv6 状态。
  - 检查 OSPF，发现 ospfd 异常时尝试恢复。
  - 拒绝不完整拓扑、缺失需求端点和缺失遥测。
  - 记录 topology SHA-256，避免不知情地比较不同快照。
  - 记录实际发送速率，拒绝发送不足或 Baseline/TE 负载不一致。
  - 使用接收端数据计算吞吐量。
  - 使用 CV 检查单阶段稳定性。
  - 使用多周期提升标准差检查重复稳定性。
  - 分阶段写 checkpoint，失败时保留已得到的数据。
  - 增加内存余量和 PSI memory stall 等待门禁。

- `udp_flow_probe.py`
  - 在容器内执行发送端/接收端 UDP 探针。
  - 保存逐流、逐秒接收和发送统计。

- `python_bgp_gateway/topo_sniffer.py`
  - 从实际容器接口和宿主机 veth 收集拓扑。
  - 读取链路 `tc` 容量/延迟和 bridge forwarding 状态。
  - 原子替换 Redis topology snapshot。
  - 扫描不完整、链路数异常或需求端点缺失时拒绝发布。

- `OpenSN-Library/tools/queue_monitor.py`
  - 从宿主机 qdisc 读取各节点接口队列统计。
  - 写入 `telemetry:queue:*` 和新鲜度时间戳。

- `python_te_solver/lyapunov_solver.py`
  - 把拓扑转换为带容量、延迟、队列代价的图。
  - 为 24 条需求生成候选路径。
  - 使用全局单路径拥塞优化，输出策略到 Redis queue。
  - 保留完整路径和 SID 信息供审计。

- `python_bgp_gateway/sr_policy_sender.py`
  - 区分 identity SID (`fd00::/16`) 和 transit SID (`fd01::/16`)。
  - 启用节点接口的 SRv6 能力。
  - 配置 SID、NDP proxy 和逐跳基础路由。
  - 同时下发 IPv6 SID 目的路由和 IPv4-over-SRv6 路由。
  - 下发后读取 route 验证实际已安装。

### 5.2 OpenSN 和 12×10 修复

- 新增 12×10 拓扑：
  `example/test_topologies/topology_config_12_10.json`。
- 文件内容经检查为：
  - instances：122。
  - Satellite：120。
  - GroundStation：2。
  - 配置 links：230。
- 新增大拓扑 NodeDaemon 配置：
  `LEO_TE_Controller/config/experiment_daemon_12_10.json`。
- `instance_capacity`：100 → 160。
- `parallel_num`：降到 2，减少同时创建容器造成的内存峰值。
- `enable_monitor=false`，`enable_code_server=false`，减少非必要资源。
- NodeDaemon 支持用环境变量 `INTERFACE` 覆盖配置中的 `ens33`。
- NodeDaemon 容器创建流程增加了重试和错误处理。
- Standard 支持 `OPENSN_FIXED_TIME`，使拓扑几何和故障步骤可复现。
- Standard 支持外部流量控制，避免自身流量发送器干扰一键测量。
- 依赖服务脚本的 CodeServer volume 已改为相对仓库根目录，不再写死
  `/home/bai/OpenSN-Library`。

### 5.3 鲁棒性与空间清理

每次 `run_full_experiment.sh` 退出时会：

- 停止本次 queue monitor。
- 停止 UDP 发送/接收探针。
- 停止残留 solver/sender。
- 默认删除 SRv6 encap 路由和 Redis 策略状态。
- 从节点共享目录打包原始流量文件。
- 清空已经归档的节点临时流文件。
- 删除本次 Python bytecode 临时目录。
- 删除退出状态的旧实验节点容器。
- 保留正在运行的 OpenSN 节点，便于下一次重复测量。

这只清理本实验产生的临时资源，不会执行 `redis-cli flushall`，也不会删除
服务器上的 Docker 镜像或其他项目数据。

---

## 6. 已有实验数据与证据

### 6.1 最终 6×11 完整结果

文件：

```text
results/result_20260915_fullflow_with_paths.json
```

关键数据：

```text
status                          completed
completed_cycles                1
topology nodes/links            68 / 246
topology up/down                 246 / 0
topology SHA-256                 468db8b6a874f7974aefac0f895b02095e53595e12fd239a7d4a7c5245ec304d
flows                           24
offered load                    76.400 Mbps
Baseline                        54.910 Mbps
Baseline CV                     0.304%
TE                              76.362 Mbps
TE CV                           0.355%
improvement                     39.068%
policy desired/applied/queued   24 / 24 / 0
IPv6 SID Ping                   24 / 24
IPv4-over-SRv6 Ping             24 / 24
phase_stable                    true
paired_load_valid               true
```

该结果 `target_met=false` 的唯一目标区间原因是当时目标设置为 17%～23%，
而真实提升约 39%，高于区间；不是运行失败。

### 6.2 第二次 6×11 完整结果

文件：

```text
results/result_20260915_fullflow_once.json
```

关键数据：

```text
Baseline                        54.917 Mbps
Baseline CV                     0.347%
TE                              76.254 Mbps
TE CV                           1.148%
improvement                     38.852%
policy desired/applied/queued   24 / 24 / 0
IPv6 SID Ping                   24 / 24
IPv4-over-SRv6 Ping             24 / 24
```

两次结果都接近 39%，说明在该 6×11 拓扑和 76.4 Mbps offered load 下，
单周期结果具有较好一致性。

### 6.3 一次资源门禁中断结果

文件：

```text
results/result_manual_20260915_131616.json
```

它只完成 Baseline：

```text
Baseline                        56.429 Mbps
Baseline CV                     0.812%
status                          in_progress
```

随后因宿主机 `memory full avg10=5.60% > 5%` 被门禁中止，算法没有执行。
该文件不能用于提升率结论，但证明资源门禁能阻止在严重内存压力下继续产生
不可靠对比。

---

## 7. 当前没有完成、必须留到服务器的工作

### 7.1 12×10 完整验收

目前仅证明 122 个节点可以成功分配和创建。仍需在服务器完成：

1. Standard 初始化和 24 条 `te:demands` 发布。
2. OSPF 全节点收敛检查。
3. 12×10 实际 topology snapshot 和 telemetry 完整性检查。
4. 24 条策略计算。
5. 24 条策略全部下发。
6. 24/24 IPv6 SID Ping。
7. 24/24 IPv4-over-SRv6 Ping。
8. Baseline 和 TE 吞吐量配对测量。

### 7.2 12%～14% 的真实校准

6×11 在 76.4 Mbps offered load 下提升约 39%，不能直接套用到 12×10。
应在 12×10 跑通后从约 `62000 Kbps` 开始进行短时负载搜索：

- 提升高于 14%：降低 `TE_DEMAND_TOTAL_KBPS`。
- 提升低于 12%：提高 `TE_DEMAND_TOTAL_KBPS`。
- 达到区间后固定参数，再做 3 周期正式验证。

负载搜索只是校准实验工作点。每一次运行仍然给 Baseline 和 TE 使用完全
相同的负载，并保留全部结果。

### 7.3 动态闭环重算仍需单独验收

现有脚本已经证明“每个实验周期主动运行一次算法并下发一批新 SRv6 策略”
可以工作。但是，下面这类真正的持续闭环还没有形成完整证据：

```text
链路状态发生变化
→ 自动检测拓扑/队列变化
→ 自动触发重新求解
→ policy signature 变化
→ 节点替换旧 SRv6 路由
→ 流量不中断或短暂收敛
```

当前 solver 和 sender 主要按一次性任务运行，而不是长期驻留的事件驱动控制器。
服务器上完成 12×10 静态验收后，应增加一个明确的 closed-loop orchestrator，
或者用 systemd/进程管理器周期触发，并为“拓扑 hash 变化才重算”加去抖和锁。

建议的动态验收：

1. 固定初始拓扑，运行一次 solver/sender，记录 policy signature。
2. 禁用一条正在使用的链路或切换 failure step。
3. 重新运行拓扑嗅探，确认 link 状态/hash 变化。
4. 触发 solver/sender。
5. 确认受影响流的 path 和 applied signature 变化。
6. 再做 24+24 Ping。
7. 记录故障前后吞吐量、丢包和收敛时间。

---

## 8. 推荐服务器规格

12×10 不应继续使用 4 GiB 内存环境。建议：

- 操作系统：Ubuntu 22.04，裸机或具备 nested/container 权限的服务器。
- CPU：至少 8 vCPU。
- 内存：至少 8 GiB，推荐 16 GiB 或更多。
- Swap：4～8 GiB，可用于容错，但不应长期大量换页。
- 磁盘：至少预留 30 GiB；如需本地重建全部 OpenSN 镜像应预留更多。
- Linux 内核需要支持 IPv6 和 SRv6 `seg6` lightweight tunnel。
- Docker 需要允许 privileged、host network、host PID 和挂载 Docker socket。
- 测量期间不要同时编译 UI、拉取镜像或运行其他高负载任务。

如果服务器本身也是受限容器，无法使用 `--privileged` 或
`/var/run/docker.sock`，则 OpenSN NodeDaemon 无法按现方案创建节点。

---

## 9. 服务器首次安装

### 9.1 安装系统依赖

```bash
sudo apt update
sudo apt install -y \
  docker.io \
  redis-server \
  python3 \
  python3-venv \
  python3-pip \
  git \
  curl \
  jq \
  ripgrep \
  iproute2 \
  procps \
  util-linux \
  build-essential

sudo systemctl enable --now docker
sudo systemctl enable --now redis-server
```

一键脚本直接调用 `docker`，当前用户需要 Docker 权限：

```bash
sudo usermod -aG docker "$USER"
```

执行后退出 SSH 并重新登录，再检查：

```bash
docker info >/dev/null
redis-cli ping
```

应看到 Redis 输出：

```text
PONG
```

### 9.2 安装 Go

`OpenSN-Library/daemon/go.mod` 当前声明 `go 1.24.0`。服务器应安装 Go 1.24
或更高版本，然后检查：

```bash
go version
```

不要只依赖 Ubuntu 22.04 默认仓库中的旧 Go 版本。如果使用支持自动 toolchain
下载的 Go，也应保证服务器可以访问 Go 模块和 toolchain 下载源。

### 9.3 创建 Python 虚拟环境

保持两个仓库是同级目录：

```bash
export LAB_ROOT="$HOME/leo-te-lab"
export OPEN_SN_DIR="$LAB_ROOT/OpenSN-Library"
export LEO_TE_DIR="$LAB_ROOT/LEO_TE_Controller"
export TE_VENV="$LAB_ROOT/.venv"

python3 -m venv "$TE_VENV"
"$TE_VENV/bin/python" -m pip install --upgrade pip wheel setuptools

"$TE_VENV/bin/python" -m pip install \
  -r "$LEO_TE_DIR/requirements.txt"

"$TE_VENV/bin/python" -m pip install \
  -r "$OPEN_SN_DIR/TopoConfigurators/Standard/requirements.txt"
```

`LEO_TE_Controller/requirements.txt` 中原来粘连的
`exabgp==4.2.21cvxpy==1.4.2` 已在本次交接修正为两行，并补充了 PyYAML。

验证关键模块：

```bash
"$TE_VENV/bin/python" - <<'PY'
import json
import networkx
import numpy
import redis
import scipy
import yaml
print("Python dependencies OK")
PY
```

### 9.4 拉取需要的 Docker 镜像

```bash
docker pull ubuntu:22.04
docker pull realssd/opensn_etcd
docker pull realssd/satellite-router
docker pull realssd/ground-station
```

检查：

```bash
docker image ls --format '{{.Repository}}:{{.Tag}}' |
  rg 'ubuntu|realssd/opensn'
```

### 9.5 编译 NodeDaemon

```bash
cd "$OPEN_SN_DIR"
make node-daemon
test -x "$OPEN_SN_DIR/NodeDaemon"
```

这里只需要构建根目录的 `NodeDaemon`，不需要生成 2.1 GiB 的
`opensn_build.tar.gz`。

### 9.6 检查 SRv6 内核能力

```bash
test -e /proc/sys/net/ipv6/conf/all/seg6_enabled
ip -6 route help 2>&1 | rg 'seg6'
```

如果第一条失败或第二条没有 `seg6`，应先更换/启用支持 SRv6 的内核，
不要继续测量。

---

## 10. 服务器上第一次运行 12×10 的完整步骤

以下步骤假设两个仓库都位于 `$HOME/leo-te-lab`。

### 10.1 设置本次终端变量

每次重新登录服务器后执行：

```bash
export LAB_ROOT="$HOME/leo-te-lab"
export OPEN_SN_DIR="$LAB_ROOT/OpenSN-Library"
export LEO_TE_DIR="$LAB_ROOT/LEO_TE_Controller"
export TE_VENV="$LAB_ROOT/.venv"
export PYTHON_BIN="$TE_VENV/bin/python"

export HOST_IFACE="$(
  ip route show default |
  awk '/default/ {print $5; exit}'
)"

printf 'LAB_ROOT=%s\nHOST_IFACE=%s\n' "$LAB_ROOT" "$HOST_IFACE"
```

`HOST_IFACE` 不能是空值：

```bash
test -n "$HOST_IFACE"
ip link show "$HOST_IFACE"
```

### 10.2 启动 Redis、Docker 和 etcd

```bash
sudo systemctl start docker
sudo systemctl start redis-server

docker rm -f opensn_etcd 2>/dev/null || true

docker run -d --rm \
  --name opensn_etcd \
  -p 2379:2379 \
  realssd/opensn_etcd

redis-cli ping
```

### 10.3 清除旧 OpenSN 实例

只针对本实验容器：

```bash
docker rm -f opensn_node_daemon 2>/dev/null || true

mapfile -t OLD_NODES < <(
  {
    docker ps -aq --filter name=Satellite_
    docker ps -aq --filter name=GroundStation_
  } | sort -u
)

if ((${#OLD_NODES[@]})); then
  docker rm -f "${OLD_NODES[@]}"
fi
```

### 10.4 启动 12×10 NodeDaemon

```bash
export TE_RUNTIME_DIR
TE_RUNTIME_DIR="$(mktemp -d /tmp/opensn-te-runtime.XXXXXX)"

docker run -d --rm \
  --name opensn_node_daemon \
  --privileged \
  --network host \
  --pid host \
  -e INTERFACE="$HOST_IFACE" \
  -e GOMEMLIMIT=256MiB \
  -e GOGC=50 \
  -v "$LAB_ROOT:$LAB_ROOT" \
  -v "$TE_RUNTIME_DIR:$TE_RUNTIME_DIR" \
  -v /var/run/docker.sock:/var/run/docker.sock \
  -w "$TE_RUNTIME_DIR" \
  --entrypoint "$OPEN_SN_DIR/NodeDaemon" \
  ubuntu:22.04 \
  "$LEO_TE_DIR/config/experiment_daemon_12_10.json" \
  experiment
```

等待 API：

```bash
for _ in $(seq 1 30); do
  if curl -fsS http://127.0.0.1:8080/api/platform/status; then
    break
  fi
  sleep 1
done

curl -fsS http://127.0.0.1:8080/api/platform/status
docker logs --tail 50 opensn_node_daemon
```

### 10.5 上传 12×10 拓扑并启动

```bash
cd "$OPEN_SN_DIR"

curl --fail-with-body -sS -X POST \
  http://127.0.0.1:8080/api/emulation/update \
  -H 'Content-Type: application/json' \
  --data-binary @example/test_topologies/emu_config.json

curl --fail-with-body -sS -X POST \
  http://127.0.0.1:8080/api/emulation/topology \
  -H 'Content-Type: application/json' \
  --data-binary @example/test_topologies/topology_config_12_10.json

curl --fail-with-body -sS -X POST \
  http://127.0.0.1:8080/api/emulation/start
```

三个响应都必须包含：

```json
"code": 0
```

等待 122 个节点：

```bash
for _ in $(seq 1 150); do
  SATELLITES="$(
    docker ps --filter name=Satellite_ --format '{{.Names}}' |
    wc -l
  )"
  GROUND_STATIONS="$(
    docker ps --filter name=GroundStation_ --format '{{.Names}}' |
    wc -l
  )"
  printf 'Satellite=%s/120 GroundStation=%s/2\n' \
    "$SATELLITES" "$GROUND_STATIONS"

  if [[ "$SATELLITES" -eq 120 && "$GROUND_STATIONS" -eq 2 ]]; then
    break
  fi
  sleep 2
done

test "$SATELLITES" -eq 120
test "$GROUND_STATIONS" -eq 2
```

检查是否有异常退出节点：

```bash
docker ps -a \
  --filter status=exited \
  --format '{{.Names}}\t{{.Status}}' |
  rg '^(Satellite|GroundStation)_' || true
```

正常情况没有输出。

### 10.6 只清理本实验的 Redis 旧数据

不要执行 `redis-cli flushall`。

```bash
redis-cli --scan --pattern 'topo:link:*' |
  xargs -r redis-cli del

redis-cli --scan --pattern 'telemetry:queue:*' |
  xargs -r redis-cli del

redis-cli del \
  topo:last_update_unix \
  telemetry:queue:last_update_unix \
  policy_queue \
  te:policy:last_signature \
  te:policy:desired \
  te:policy:changed_at \
  te:policy:applied_signature \
  te:policy:applied \
  te:mode \
  te:demands \
  te:flow:dst_ip

redis-cli set te:traffic:external_control 1
```

### 10.7 启动 Standard，等待需求和 OSPF 初始化

```bash
cd "$OPEN_SN_DIR/TopoConfigurators/Standard"

nohup env \
  PROTOCOL_BUFFERS_PYTHON_IMPLEMENTATION=python \
  PYTHONPATH="$OPEN_SN_DIR/TopoConfigurators" \
  ADDR=127.0.0.1 \
  PORT=8080 \
  OPENSN_FIXED_TIME=2026-09-14T16:25:00 \
  "$PYTHON_BIN" -u main.py \
  > /tmp/standard_12_10.log 2>&1 &

export STANDARD_PID=$!
printf 'Standard PID=%s\n' "$STANDARD_PID"
```

等待 24 条 demand：

```bash
for _ in $(seq 1 180); do
  DEMAND_COUNT="$(
    redis-cli --raw get te:demands |
    "$PYTHON_BIN" -c \
      'import json,sys
s=sys.stdin.read().strip()
print(len(json.loads(s)) if s else 0)'
  )"
  printf 'te:demands=%s/24\n' "$DEMAND_COUNT"

  if [[ "$DEMAND_COUNT" -eq 24 ]]; then
    break
  fi
  sleep 2
done

test "$DEMAND_COUNT" -eq 24
tail -n 80 /tmp/standard_12_10.log
```

需求出现后，再给 OSPF 约 60 秒收敛：

```bash
sleep 60
```

### 10.8 停掉初始化控制进程，保留数据面节点

这样可以释放内存，也能保证 Baseline 期间 Standard 不改变几何或主动发流：

```bash
kill "$STANDARD_PID" 2>/dev/null || true
wait "$STANDARD_PID" 2>/dev/null || true

docker stop --time 10 opensn_node_daemon
```

再次确认节点仍在：

```bash
test "$(
  docker ps --filter name=Satellite_ --format '{{.Names}}' |
  wc -l
)" -eq 120

test "$(
  docker ps --filter name=GroundStation_ --format '{{.Names}}' |
  wc -l
)" -eq 2
```

### 10.9 先做一次 12×10 冒烟实验

第一次只要求真实跑通，不要求立刻命中 12%～14%：

```bash
cd "$LEO_TE_DIR"

OPEN_SN_DIR="$OPEN_SN_DIR" \
PYTHON_BIN="$PYTHON_BIN" \
TE_CYCLES=1 \
TE_MEASURE_SECONDS=15 \
TE_DEMAND_TOTAL_KBPS=62000 \
TE_TARGET_LOW_PERCENT=12 \
TE_TARGET_HIGH_PERCENT=14 \
TE_MIN_AVAILABLE_MIB=1024 \
TE_RESOURCE_WAIT_SECONDS=180 \
./run_full_experiment.sh
```

完整运行一般约 2～4 分钟。`--report-only` 已由包装脚本使用，所以只要流程
完整、数据有效，即使提升不在 12%～14%，命令也会正常结束并保留真实结果。

### 10.10 查看全部结果

```bash
cat "$LEO_TE_DIR/results/latest/summary.txt"

"$PYTHON_BIN" -m json.tool \
  "$LEO_TE_DIR/results/latest/result.json" |
  less

less "$LEO_TE_DIR/results/latest/console.log"
less "$LEO_TE_DIR/results/latest/queue_monitor.log"
```

最近一次运行目录：

```bash
readlink -f "$LEO_TE_DIR/results/latest"
```

---

## 11. 12%～14% 校准和正式测试

### 11.1 短时搜索

建议顺序测试总负载：

```text
55000 Kbps
62000 Kbps
70000 Kbps
```

每次只改 `TE_DEMAND_TOTAL_KBPS`，其他条件全部保持一致。例如：

```bash
OPEN_SN_DIR="$OPEN_SN_DIR" \
PYTHON_BIN="$PYTHON_BIN" \
TE_CYCLES=1 \
TE_MEASURE_SECONDS=15 \
TE_DEMAND_TOTAL_KBPS=62000 \
TE_TARGET_LOW_PERCENT=12 \
TE_TARGET_HIGH_PERCENT=14 \
TE_MIN_AVAILABLE_MIB=1024 \
TE_RESOURCE_WAIT_SECONDS=180 \
"$LEO_TE_DIR/run_full_experiment.sh"
```

判断规则：

- 提升 `>14%`：下一次降低负载。
- 提升 `<12%`：下一次提高负载。
- 提升在 `12%～14%`：记录该负载，再做一次独立确认。
- 如果提升随负载不单调，先检查 topology hash、主机资源、发送负载和 CV，
  不要继续盲目调参。

每次运行都会产生新目录，不会覆盖前一次数据。把负载值和结果目录一起记录。

### 11.2 冻结参数

找到大约 13% 的工作点后：

1. 停止搜索。
2. 固定 topology、fixed time、failure step、24 条 demand 和总负载。
3. 不再根据正式结果继续微调。
4. 做 3 周期正式实验。

正式命令：

```bash
OPEN_SN_DIR="$OPEN_SN_DIR" \
PYTHON_BIN="$PYTHON_BIN" \
TE_CYCLES=3 \
TE_MEASURE_SECONDS=20 \
TE_DEMAND_TOTAL_KBPS=<已经冻结的真实负载> \
TE_TARGET_LOW_PERCENT=12 \
TE_TARGET_HIGH_PERCENT=14 \
TE_MIN_AVAILABLE_MIB=1024 \
TE_RESOURCE_WAIT_SECONDS=180 \
"$LEO_TE_DIR/run_full_experiment.sh"
```

`TE_MEASURE_SECONDS=20`、3 周期兼顾稳定性和总时长。若服务器空闲但 CV 仍高，
可提高到 25～30 秒，不应通过缩短到 10 秒以下换取偶然结果。

### 11.3 正式验收条件

`result.json` 应同时满足：

```text
status = completed
completed_cycles = 3
target_met = true
all_cycles_in_target = true
phase_stable = true
paired_load_valid = true
improvement_stddev_percent <= 3
每周期 policy = 24/24/0
每周期 IPv6 SID Ping = 24/24
每周期 IPv4-over-SRv6 Ping = 24/24
每阶段 sent_load_ratio >= 0.98
每周期 Baseline/TE 实际发送负载差 <= 1%
每阶段 CV <= 5%
```

还应确认三周期 topology snapshot hash 相同。如果以后改成动态拓扑实验，则必须
把每次 hash、故障事件时间和重算时间单独记录，不能与静态稳定性实验混在一起。

---

## 12. 数据存放位置

每次运行：

```text
LEO_TE_Controller/results/runs/YYYYMMDD_HHMMSS/
```

最近一次：

```text
LEO_TE_Controller/results/latest
```

主要文件：

```text
result.json
  完整机器可读结果；逐周期、逐流、路径、策略、Ping、CV、负载和拓扑摘要。

summary.txt
  中文简要结论。

console.log
  一键流程的完整控制台输出。

queue_monitor.log
  队列监控输出。

runtime/baseline_1.json
runtime/te_1.json
  Baseline 和 TE 接收端/发送端逐流与逐秒数据。

runtime/solver.log
runtime/sender.log
  算法和策略下发日志。

raw_flow_files.tar.gz
  从节点共享目录归档的原始探针文件。

recovered_tmp_before_run.tar.gz
recovered_raw_before_run.tar.gz
  仅当新一轮开始前发现上次异常遗留文件时生成。
```

正式结果建议另外备份到服务器独立目录：

```bash
export ARCHIVE_DIR="$HOME/leo-te-final-results"
mkdir -p "$ARCHIVE_DIR"

cp -aL "$LEO_TE_DIR/results/latest" \
  "$ARCHIVE_DIR/12x10-final-$(date +%Y%m%d_%H%M%S)"
```

不要把大批正式运行数据直接提交到 Git。最终报告需要的小型 JSON 可以在检查后
单独提交，原始归档宜放服务器数据盘或对象存储。

---

## 13. 常用排错命令

### 13.1 NodeDaemon API 不通

```bash
docker ps -a --filter name=opensn_node_daemon
docker logs --tail 200 opensn_node_daemon
ss -lntp | rg ':8080'
```

重点检查：

- NodeDaemon 是否已编译。
- bind mount 后容器内路径是否存在。
- `HOST_IFACE` 是否是服务器真实默认出口网卡。
- 8080 端口是否被其他程序占用。

### 13.2 再次出现 unable to allocate instances

```bash
jq '.app.instance_capacity, .app.parallel_num' \
  "$LEO_TE_DIR/config/experiment_daemon_12_10.json"
```

应输出：

```text
160
2
```

再确认启动 NodeDaemon 时传入的是
`experiment_daemon_12_10.json`，不是 `experiment_daemon.json`。

### 13.3 节点数量不足

```bash
docker ps -a \
  --format '{{.Names}}\t{{.Status}}\t{{.Image}}' |
  rg '^(Satellite|GroundStation)_'

docker logs --tail 300 opensn_node_daemon

free -h
cat /proc/pressure/memory
docker stats --no-stream
```

如果出现 OOM/大量 Swap：

- 停止无关服务。
- 确保 monitor 和 CodeServer 已关闭。
- 不要在建拓扑时同时运行编译或吞吐量实验。
- 增加服务器内存，而不是反复重试并接受不完整节点。

### 13.4 Standard 没有发布 24 条 demand

```bash
tail -n 200 /tmp/standard_12_10.log
redis-cli --raw get te:demands
docker ps --filter name=Satellite_ --format '{{.Names}}' | wc -l
curl -fsS http://127.0.0.1:8080/api/platform/status
```

重点检查：

- Standard Python 依赖和 protobuf 版本。
- `PYTHONPATH` 是否指向当前 OpenSN 仓库。
- NodeDaemon 是否仍在运行。
- 是否已经创建完整 120+2 节点。

### 13.5 一键脚本提示 telemetry 不完整

```bash
tail -n 200 "$LEO_TE_DIR/results/latest/queue_monitor.log"
redis-cli --scan --pattern 'telemetry:queue:*' | wc -l
redis-cli get telemetry:queue:last_update_unix
```

确认当前用户有 Docker 权限，且 OpenSN 的 veth/qdisc 可以从宿主机读取。

### 13.6 OSPF 不健康

```bash
FIRST_SAT="$(
  docker ps --filter name=Satellite_ --format '{{.Names}}' |
  head -n 1
)"

docker exec "$FIRST_SAT" ps -ef | rg ospfd
docker exec "$FIRST_SAT" ip route show proto ospf | wc -l
docker exec "$FIRST_SAT" vtysh -c 'show ip ospf neighbor'
```

一键脚本会尝试恢复死亡的 `ospfd`，但如果大量节点都无路由，应回到 OpenSN
拓扑初始化阶段排查，不能跳过门禁。

### 13.7 SRv6 下发或 Ping 失败

```bash
cat "$LEO_TE_DIR/results/latest/runtime/sender.log"

redis-cli hlen te:policy:desired
redis-cli hlen te:policy:applied
redis-cli llen policy_queue

docker exec "$FIRST_SAT" ip -6 route show | rg 'seg6|fd00|fd01'
docker exec "$FIRST_SAT" ip route show | rg 'seg6'
```

期望：

```text
desired = 24
applied = 24
queue = 0
```

### 13.8 测量中途资源门禁失败

```bash
free -h
cat /proc/pressure/memory
docker stats --no-stream
cat "$LEO_TE_DIR/results/latest/summary.txt"
less "$LEO_TE_DIR/results/latest/console.log"
```

不要关闭稳定性/资源门禁来获得一个表面“成功”的结果。应先释放资源或换更大
服务器，然后重跑。

---

## 14. 日常再次运行与最终释放资源

如果 120+2 节点仍然运行、Redis 中仍有 24 条 demand，平时只需：

```bash
export LAB_ROOT="$HOME/leo-te-lab"
export OPEN_SN_DIR="$LAB_ROOT/OpenSN-Library"
export LEO_TE_DIR="$LAB_ROOT/LEO_TE_Controller"
export PYTHON_BIN="$LAB_ROOT/.venv/bin/python"

OPEN_SN_DIR="$OPEN_SN_DIR" \
PYTHON_BIN="$PYTHON_BIN" \
TE_CYCLES=1 \
TE_MEASURE_SECONDS=15 \
TE_DEMAND_TOTAL_KBPS=<当前实验负载> \
TE_TARGET_LOW_PERCENT=12 \
TE_TARGET_HIGH_PERCENT=14 \
"$LEO_TE_DIR/run_full_experiment.sh"
```

一天实验结束后，如果下次还要继续，保留节点即可。一键脚本已经清理临时探针
和策略。

如果近期不再实验，需要释放节点、依赖服务和 runtime：

```bash
mapfile -t EXPERIMENT_NODES < <(
  {
    docker ps -aq --filter name=Satellite_
    docker ps -aq --filter name=GroundStation_
  } | sort -u
)

if ((${#EXPERIMENT_NODES[@]})); then
  docker rm -f "${EXPERIMENT_NODES[@]}"
fi

docker rm -f \
  opensn_node_daemon \
  opensn_etcd \
  opensn_influxdb \
  opensn_codeserver \
  2>/dev/null || true

docker system df
```

确认不再需要临时 NodeDaemon 工作目录后，再按其明确路径删除。不要使用宽泛的
递归删除命令，也不要为了释放少量空间删除仍会复用的节点镜像。

---

## 15. 后续开发优先级

### P0：服务器复现

- 克隆两个交接分支。
- 安装依赖和构建 NodeDaemon。
- 创建 120+2 节点。
- 获取 24 条 demand。
- 跑通一次完整 12×10 冒烟测试。

### P1：12×10 功能验收

- topology/telemetry 完整。
- solver 成功。
- policy 24/24/0。
- 两类 SRv6 Ping 都是 24/24。
- Baseline/TE 都完成且负载配对有效。

### P2：真实 12%～14% 校准

- 用短周期搜索 offered load。
- 冻结参数。
- 3 周期正式复验。
- 保留全部结果，不后改 JSON。

### P3：动态闭环

- 增加 topology/queue change detector。
- 增加事件去抖、全局运行锁和 policy signature 比较。
- 自动重跑 solver/sender。
- 测量策略替换和流量恢复时间。

### P4：工程化

- 把“依赖、建拓扑、Standard 初始化、一键实验”进一步合并为服务器 bootstrap。
- 使用 systemd 或容器编排管理 Redis、etcd、NodeDaemon 和控制器。
- 增加 12×10 自动化测试和故障注入测试。
- 将正式结果元数据与代码 Git commit、服务器硬件信息绑定。

---

## 16. 已知迁移注意事项

1. `run_full_experiment.sh` 的默认 OpenSN 位置是假定两个仓库同级，因此建议保持：

   ```text
   leo-te-lab/
   ├── OpenSN-Library/
   └── LEO_TE_Controller/
   ```

   放在其他位置时显式设置 `OPEN_SN_DIR`。

2. OpenSN JSON 中仍保留 `interface_name: ens33`，但 NodeDaemon 已支持
   `INTERFACE` 环境变量；服务器必须传真实 `HOST_IFACE`。

3. LEO 的 `python_bgp_gateway/exabgp_conf.ini` 仍有原虚拟机绝对路径。
   当前一键实验不使用该 ExaBGP 配置；以后启用 ExaBGP 时必须改成服务器路径。

4. 部分早期 README 仍保留 `/home/bai` 命令，用于记录原虚拟机执行历史。
   服务器执行以本文档中的 `$LAB_ROOT` 命令为准。

5. OpenSN 的 GitHub 仓库不包含本地 Docker image tar 和构建目录。服务器需
   `docker pull`，不能期待 clone 后自动出现镜像。

6. 12×10 的最终实际有向链路数量要以 `topo_sniffer.py` 发布结果为准；
   topology JSON 的 230 条配置 link 和运行时的有向 link 不是同一个计数概念。

7. 原虚拟机上最后已停止 OpenSN 节点和实验进程。Redis 中残留的是旧 6×11
   topology/scenario 元数据，不是可继续使用的 12×10 运行状态。

---

## 17. 最短交接清单

服务器接手人按以下顺序即可：

```text
1. clone 两个 server-handoff-20260915 分支
2. 安装 Docker、Redis、Python venv、Go 1.24+
3. pip 安装两个 requirements
4. docker pull 四个镜像
5. make node-daemon
6. 检查 HOST_IFACE 和 SRv6 内核能力
7. 用 experiment_daemon_12_10.json 启动 NodeDaemon
8. POST emu_config + topology_config_12_10 + start
9. 确认 120 Satellite + 2 GroundStation
10. 启动 Standard，确认 te:demands=24
11. 停 Standard 和 NodeDaemon，保留节点
12. 以 62000 Kbps、15 秒、1 周期做冒烟测试
13. 查看 results/latest 下全部数据
14. 校准 offered load 到真实 12%～14%
15. 固定负载后执行 20 秒、3 周期正式验证
16. 再开发和验证链路变化后的自动闭环重算
```

本文档中的“已完成”只对应有真实日志和 JSON 证据的部分；12×10 吞吐量和
12%～14% 目标明确保留为服务器后续任务。
